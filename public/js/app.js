/**
 * Transitly — Legacy Script Compatibility Layer
 * Forward compatibility wrapper for modular site architecture.
 */
document.addEventListener('DOMContentLoaded', () => {
  // Safe fallback if any legacy cache hits this file
  console.log('Transitly modular architecture active.');
});
